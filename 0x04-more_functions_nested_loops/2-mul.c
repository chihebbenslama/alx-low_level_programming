#include <stdio.h>
#include "main.h"
#include <ctype.h>

/**
 * mul - multiplies two integers.
 * @a: integer to multiply
 * @b: integer to multiply
 * Return: Returns the result of multipliying a and b.
*/
int mul(int a, int b)
{
	return (a * b);
}
