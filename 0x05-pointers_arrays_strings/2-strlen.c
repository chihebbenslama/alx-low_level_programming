#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - returns the length of a string.
 * @s: string to be checked
 *
 *
 * Return:  length of s
*/
int _strlen(char *s)
{
	int length = strlen(*s);

	return (length);
}
