#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * main - Prints a random number and states whether
 * it is positive, negative, or zero.
 *
 *
*/
int puts_half(char *str)
{
	int len = _strlen(str);

	return (len);
}
