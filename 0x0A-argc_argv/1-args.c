#include <stdio.h>

/**
 * main -prints the number of arguments passed into it.
 * @argc: arguments count
 * @argv: arguments
 *
 * Return: 0
*/

int main(int argc, *argv[])
{
	printf("%d\n", argc);
	return (0);
}
