#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer.
 * @n: the integer to check
 *
 * Return: the abs of an integer
*/
int _abs(int n)
{
	if (n < 0)
	{
		return (n * n);
	} return (n);
}
