#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @x: integer.
 * @y: integer.
 * Return: returns the result of adding two integers
*/
int add(int x, int y)
{
	int w;

	w = y + x;
	return (w);
}
